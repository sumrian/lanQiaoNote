function replaceSpace (s) {
    // 1.  return encodeURI(s)
    // 2.  return s.split(' ').join('%20');
    // 3.  return s.replaceAll(' ', '%20')
    // 4.  return s.replace(/\s/g,'%20')
 }
 
 module.exports = replaceSpace